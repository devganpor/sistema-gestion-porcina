[phases.setup]
nixPkgs = ['nodejs_18', 'npm-9_x']
aptPkgs = ['python3', 'build-essential']

[phases.install]
cmds = ['cd server && npm install --production=false']

[phases.build]
cmds = ['echo "Build completed"']

[start]
cmd = 'cd server && npm run railway:start'