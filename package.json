{
  "name": "sgp-railway-deploy",
  "version": "1.0.1",
  "scripts": {
    "build": "echo 'Server-only deployment'",
    "start": "cd server && npm start"
  },
  "engines": {
    "node": "18.x"
  }
}
